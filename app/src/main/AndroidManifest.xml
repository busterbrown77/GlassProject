<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mstratton.jplapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="20" />

    <uses-permission android:name="com.google.android.glass.permission.DEVELOPMENT" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <!-- Don't use themes -->
    <application
        android:allowBackup="true">

        <activity
            android:name=".ViewFinder"
            android:enabled="true"
            android:icon="@drawable/ic_camera"
            android:label="@string/viewfinder_label" >

            <intent-filter>
                <action android:name="com.google.android.glass.action.VOICE_TRIGGER" />
            </intent-filter>

            <!-- Voice command found in res/xml/voice_trigger_start -->
            <meta-data
                android:name="com.google.android.glass.VoiceTrigger"
                android:resource="@xml/voice_trigger_start" />
        </activity>

        <activity
            android:name=".PartHistory"
            android:enabled="true"
            android:icon="@drawable/ic_camera"
            android:label="@string/parthistory_label" >

            <intent-filter>
                <action android:name="com.google.android.glass.action.VOICE_TRIGGER" />
            </intent-filter>

            <!-- Voice command found in res/xml/voice_trigger_start -->
            <meta-data
                android:name="com.google.android.glass.VoiceTrigger"
                android:resource="@xml/voice_trigger_start" />
        </activity>

        <activity
            android:name=".PartView"
            android:enabled="true"
            android:label="@string/partview_label" >
        </activity>

        <service
            android:name=".Service"
            android:enabled="true"
            android:exported="true"
            android:label="@string/app_name" />
    </application>
</manifest>